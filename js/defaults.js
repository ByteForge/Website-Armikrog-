// Site defaults
    // Tween specifics
    var useTweens = true;
    var tweenEasing = Expo.easeOut;
    var tweenDuration = 0.40;

    // Auth field specifics
    var authUserFieldTweenCSS = {
        open:   {css: {marginTop:   0, opacity: 1}},
        close:  {css: {marginTop: -10, opacity: 0}}
    }
    var authPassFieldTweenCSS = {
        open:   {css: {marginTop:   0, opacity: 1}},
        close:  {css: {marginTop: -15, opacity: 0}}
    }
